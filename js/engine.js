#alert("Привет")

/*
function print_object(obj){
    var res= "<ul>"
    for(i in obj)
        res += '<li>' + i + ': ' + obj[i] + '</li>';
    res += '</ul>';
    document.write(res);
}
var text = document.querySelector("#myId")
print_object(text)
*/
